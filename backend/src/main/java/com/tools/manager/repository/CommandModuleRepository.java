package com.tools.manager.repository;

import com.tools.manager.entity.CommandModule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CommandModuleRepository extends JpaRepository<CommandModule, Long> {
}
